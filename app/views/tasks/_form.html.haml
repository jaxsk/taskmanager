= simple_form_for(@task) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :description
    - if @task.id
      = f.input :done

  .form-actions
    = f.button :submit, class: 'btn btn-primary btn-block'
